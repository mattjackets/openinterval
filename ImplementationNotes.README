All camera support files:
 * are allowed use of registers 21-25
 * must have a 'shutterrelease' subroutine and the 'ret' instruction as their final instruction
 * must use 'irport' to identify the port to which the IR LED is attached
 * must use 'irpin' to identify the pin of 'irport' to which the IR LED is attached
